import { createMuiTheme } from "@material-ui/core/styles";

const palette = {
  primary: { main: "#ffffff", contrastText: "#000000" },
  secondary: { main: "#FEE04E" }
};

export default createMuiTheme({ palette });
